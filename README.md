# builder-design-pattern

A Java implementation of the Builder Pattern to create `Student` objects.

## Key Features
- **Validation**: Ensures students are at least 25 years old.
- **Fluent API**: Methods return `this` for easy chaining.
- **Encapsulation**: Keeps the construction logic separate from the data.
